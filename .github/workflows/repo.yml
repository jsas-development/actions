name: Git Test

on:
  pull_request:
    types: [assigned, opened, synchronize, reopened]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Test
        run: |
          ls -la
          set CURRENT_BRANCH=${GITHUB_REF##*/}
          echo running on branch ${GITHUB_REF##*/}
          echo sucesso, agora estamos em: $CURRENT_BRANCH
